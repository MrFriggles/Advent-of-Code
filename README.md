# Advent Of Code

## How to build
Once the repository has been cloned and entered:
```
cmake -S . -B ./build
make -C ./build
```
From there, the executable `aoc` will be built.

## Usage
```
# aoc [-d/--day=<day> | -a/--all]
```
